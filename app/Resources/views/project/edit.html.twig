{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Project edit</h1>

        {{ form_start(edit_form) }}
            {{ form_widget(edit_form) }}
            <ul class="list-inline">
                <li>
                    <a class="btn btn-default" href="{{ path('project_show', { 'id': project.id }) }}">zurück</a>
                </li>
                <li>
                    <input type="submit" value="Speichern" class="btn btn-primary" />
                </li>
            </ul>
        {{ form_end(edit_form) }}
        
    </div>
{% endblock %}
